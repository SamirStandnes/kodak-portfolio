# Core dependencies
pandas
numpy
PyYAML

# Data fetching
yfinance
openpyxl

# Dashboard
# Pinned: every Heroku rebuild installs fresh, and unpinned streamlit pulled in
# a breaking upgrade (1.52 st.Page path resolution). Bump deliberately.
streamlit~=1.52.2
streamlit-aggrid
plotly

# CLI formatting
rich
tabulate

# PostgreSQL (for Heroku deployment)
psycopg2-binary

# Testing (development)
pytest
pytest-cov
